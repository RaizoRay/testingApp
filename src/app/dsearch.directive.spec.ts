import { DsearchDirective } from './dsearch.directive';

describe('DsearchDirective', () => {
  it('should create an instance', () => {
    const directive = new DsearchDirective();
    expect(directive).toBeTruthy();
  });
});
